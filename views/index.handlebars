
<div class="row">
  <div class="col-md-2"></div>

  <div class="col-md-4" id="false">
     
<ul>
  {{#each burgers}}
    {{#unless this.devoured}}
      <li>
        {{this.burger_name}}
        <p>                                           </p>
        <form action="burgers/update/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden"  id="button" name="devoured" value="true">
          <button type="submit">devoured?</button>
        </form>
      </li>
    {{/unless}}
  {{/each}}
</ul>

</div>

  <div class="col-md-4" id="true">
 

<ul>
  {{#each burgers}}
    {{#if this.devoured}}
      <li>
        {{this.burger_name}}

      </li>
    {{/if}}
  {{/each}}
</ul>
</div>

 <div class="col-md-2"></div>
</div>

<br><Br>
<form class="create-update-form clearfix" action="burgers/create" method="POST">

          <div class="form-group">
            <label for="burgerInput">Enter a burger you want to eat and click Submit!</label><br><br>
            <input type="text" id="burgerInput" name="name">
          </div>



  <button type="submit" class="btn btn-danger">Submit!</button>
</form>